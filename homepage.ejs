<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>blogs home page</title>
</head>
<body>
    <% posts.forEach(function(items) { %>
        <li> <a href="/posts/<%= items.id%>"><%= items.title %></a></li>
    <% }); %>
</body>
</html>